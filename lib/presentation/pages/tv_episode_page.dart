import 'package:flutter/material.dart';

class TvEpisodePage extends StatelessWidget {
  const TvEpisodePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
